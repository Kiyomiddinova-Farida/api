import React from 'react'

const SettingsPreferences: React.FC = () => {
  return <div className="p-4 rounded bg-white dark:bg-gray-800 shadow">Preference settings</div>
}

export default React.memo(SettingsPreferences)