import React from 'react'

const SettingsProfile: React.FC = () => {
  return <div className="p-4 rounded bg-white dark:bg-gray-800 shadow">Profile settings</div>
}

export default React.memo(SettingsProfile)