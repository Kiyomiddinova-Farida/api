import React from 'react'

const SupportTickets: React.FC = () => {
  return <div className="p-4 rounded bg-white dark:bg-gray-800 shadow">Support tickets</div>
}

export default React.memo(SupportTickets)